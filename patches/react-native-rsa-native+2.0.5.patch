diff --git a/node_modules/react-native-rsa-native/android/build.gradle b/node_modules/react-native-rsa-native/android/build.gradle
index 82f60d3..0b31a53 100644
--- a/node_modules/react-native-rsa-native/android/build.gradle
+++ b/node_modules/react-native-rsa-native/android/build.gradle
@@ -9,15 +9,12 @@ buildscript {
     if (project == rootProject) {
         repositories {
             google()
-            jcenter()
-            maven {
-                url "https://maven.fabric.io/public"
-            }
             mavenCentral()
         }
 
         dependencies {
-            classpath("com.android.tools.build:gradle:3.5.3")
+            // Standalone Android builds for this library are not supported in this app repo.
+            // The app's root buildscript provides the Android Gradle Plugin.
         }
     }
 }
@@ -45,7 +42,6 @@ allprojects {
     repositories {
         mavenLocal()
         google()
-        jcenter()
         maven {
             // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
             url "$rootDir/../node_modules/react-native/android"
