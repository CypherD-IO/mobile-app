// Customer.io secrets template — copy to CioSecrets.xcconfig and fill in values.
// CioSecrets.xcconfig is gitignored; this example is committed as a reference.
CUSTOMERIO_CDP_API_KEY = YOUR_CDP_API_KEY_HERE
