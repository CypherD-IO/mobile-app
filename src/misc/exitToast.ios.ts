/**
 * Platform-specific toast helper (iOS).
 *
 * No-op by design. We keep the export shape identical to the Android implementation.
 */
export const showExitToast = (): void => {
  // Intentionally empty: Android-style toasts are not used on iOS.
};

